version: 2

models:
  - name: int_src_local_bike_product_daily
    description: '{{ doc("int_src_local_bike_product_daily") }}'
    columns:
      - name: product_id
        description: "The product_id."
        data_tests:
          - not_null